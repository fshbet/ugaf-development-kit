# Examples

The reference example plugin lives at `games/example_game/` (not under this
directory) — it exercises the full `GamePlugin` lifecycle without performing real
automation. See `PLUGIN_ARCHITECTURE.md` for the plugin layout and
`templates/plugin.py` / `templates/manifest.yaml` for a copy-paste starting point for
a new plugin.
