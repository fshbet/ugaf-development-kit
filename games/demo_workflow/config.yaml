# Local override for this plugin's own InputManager — independent of the
# shared framework config, so the demo runs safely (no real desktop/device
# automation) out of the box. Switch to "adb" and set adb.default_device
# to drive a real connected Android device instead.
input:
  provider: mock
